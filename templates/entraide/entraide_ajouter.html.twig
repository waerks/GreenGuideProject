{% include "header/header.html.twig" %}


<section>
    <div class="container">
        <section id="recetteATitre">
            <div class="divTitre">
                <h2>Poser vos questions aux autres utilisateurs !</h2>
            </div>
        </section>
        <section id="aideAjout">
            {{ form_start(form, { 'attr': {'enctype': 'multipart/form-data'} }) }}
                <h3>Votre question</h3>
                <hr>
                
                {{ form_label(form.titre, 'Le titre de votre question') }}
                {{ form_widget(form.titre, {'attr': {'class': 'input'}}) }}

                {{ form_label(form.image, 'Photo de votre question') }}
                {{ form_widget(form.image, {'attr': {'class': 'input'}}) }}

                {{ form_label(form.contenu, 'Votre question') }}
                {{ form_widget(form.contenu, {'attr': {'class': 'input'}}) }}
                
                <button type="submit" id="mainBtn">Poster</button>
            {{ form_end(form) }}
        </section>
    </div>
</section>

{% include "footer/footer.html.twig" %}